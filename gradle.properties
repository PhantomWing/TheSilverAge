# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx3G
# Architectury Loom requires the Gradle daemon itself to run on JDK 21 (the previous
# NeoForge ModDev plugin only required a Java 21 *toolchain* and tolerated a JDK 17
# launcher). The system default `java` here is JDK 17, so the daemon JVM is pinned
# explicitly. Adjust this path (or override in a non-committed gradle.properties /
# via -Dorg.gradle.java.home=) if your JDK 21 lives elsewhere.
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-21.0.7.6-hotspot
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# NOTE: Architectury Loom is not yet compatible with the Gradle configuration cache,
# so it is disabled here (it was enabled under the previous NeoForge ModDev setup).
org.gradle.configuration-cache=false

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.4
parchment_mappings_version=2025.03.23

# Minecraft — 1.21.4 (the "Pale Garden" drop).
minecraft_version=1.21.4
minecraft_version_range=[1.21.4,1.21.5)
neo_version=21.4.157
neo_version_range=[21.4,)
loader_version_range=[4,)

# Architectury / multi-loader. The 1.21.4 line needs architectury-loom 1.10.x
# (1.7.x topped out at 1.21.3) and Architectury API 15.x (was 14.x for 1.21.2/3).
# The architectury-plugin (3.4.x) is MC-agnostic and stays put.
architectury_plugin_version=3.4.164
architectury_loom_version=1.10.455
architectury_version=15.0.3
enabled_platforms=fabric,neoforge

# Fabric
fabric_loader_version=0.16.14
fabric_api_version=0.119.4+1.21.4

## Mod info
mod_version=1.3.0
mod_id=thesilverage
mod_group_id=com.phantomwing.thesilverage
mod_license=MIT License
mod_name=The Silver Age
mod_description=A vanilla-friendly expansion that introduces (oxidizable) silver, designed and polished to feel like an official game drop.
mod_authors=PhantomWing

# Other dependencies (dev-runtime niceties — runtime only unless noted).
jade_neoforge_version=17.3.0+neoforge
# JEI (NeoForge only — recipe viewer, dev-runtime). 20.0.0.4 is the 1.21.4 build.
# No JEI-Fabric or EMI build surfaced for 1.21.4, so Fabric's recipe viewer is deferred.
jei_neoforge_version=20.0.0.4
# Cloth Config (Fabric only, REAL dep) — backs the Fabric AutoConfig config.
# 17.0.144 is the Cloth Config build for the 1.21.4 line (was 16.0.143 on 1.21.3).
cloth_config_version=17.0.144

# --- 1.21.4 PORT NOTES ----------------------------------------------------------
# Create + Farmer's Delight (NeoForge) are STILL NOT published for 1.21.4, so their
# compat stays dropped:
#   * Create: compat code removed; silver_sheet + the c:plates tags stay (vanilla),
#     dormant until Create ships for 1.21.4+.
#   * Farmer's Delight (NeoForge): Silver Knife compiles against the local compile-only
#     KnifeItem stub (no FD jar). Knife = SwordItem fallback until FD ships.
# FD Refabricated (Fabric) is consumed via the same stub, so no FDR jar is needed.
#
# AppleSkin: DROPPED on this branch — AppleSkin has no 1.21.4 build (it skipped
# 1.21.3 → 1.21.5). Re-add appleskin_*_version when a 1.21.4 (or compatible) build exists.
#
# JEI: re-added on NeoForge (20.0.0.4). EMI + JEI-Fabric: still deferred — no confirmed
# 1.21.4 build surfaced. Re-add the Fabric recipe-viewer wiring once available.
# --------------------------------------------------------------------------------
