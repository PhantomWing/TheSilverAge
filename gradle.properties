## Mod info
mod_version=1.3.2
mod_id=thesilverage
mod_group_id=com.phantomwing.thesilverage
mod_license=MIT License
mod_name=The Silver Age
mod_description=A vanilla-friendly expansion that introduces (oxidizable) silver, designed and polished to feel like an official game drop.
mod_authors=PhantomWing

# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx3G
# Architectury Loom requires the Gradle daemon itself to run on JDK 21 (the previous
# NeoForge ModDev plugin only required a Java 21 *toolchain* and tolerated a JDK 17
# launcher). The system default `java` here is JDK 17, so the daemon JVM is pinned
# explicitly. Adjust this path (or override in a non-committed gradle.properties /
# via -Dorg.gradle.java.home=) if your JDK 21 lives elsewhere.
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-21.0.7.6-hotspot
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# NOTE: Architectury Loom is not yet compatible with the Gradle configuration cache,
# so it is disabled here (it was enabled under the previous NeoForge ModDev setup).
org.gradle.configuration-cache=false

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.5
parchment_mappings_version=2025.06.15

# Minecraft
minecraft_version=1.21.5
minecraft_version_range=[1.21.5,1.21.6)
neo_version=21.5.97
neo_version_range=[21.5,)
loader_version_range=[4,)

# Architectury / multi-loader. The 1.21.5 line needs architectury-loom 1.11.x+
# (1.10.x topped out at 1.21.4; 1.7.x at 1.21.3) and Architectury API 16.x (was
# 15.x for 1.21.4). The architectury-plugin (3.4.x) is MC-agnostic and stays put.
architectury_plugin_version=3.4.164
architectury_loom_version=1.14.476
architectury_version=16.1.4
enabled_platforms=fabric,neoforge

# Fabric
fabric_loader_version=0.16.14
fabric_api_version=0.128.2+1.21.5

# Publishing (Modrinth + CurseForge via mod-publish-plugin). Project IDs are public; tokens
# come from MODRINTH_TOKEN / CURSEFORGE_TOKEN env vars or modrinth_token / curseforge_token
# in ~/.gradle/gradle.properties.
mod_publish_plugin_version=2.0.0
modrinth_project_id=LEHsZZ4j
curseforge_project_id=1355287



# Other dependencies (dev-runtime niceties — runtime only unless noted).
jade_neoforge_version=18.2.0+neoforge
# JEI (NeoForge only — recipe viewer, dev-runtime). 21.4.0.27 is the 1.21.5 build
# (JEI's own version stream lags the MC number). No JEI-Fabric or EMI build surfaced
# for 1.21.5, so Fabric's recipe viewer stays deferred.
jei_neoforge_version=21.4.0.27
# Cloth Config (Fabric only, REAL dep) — backs the Fabric AutoConfig config.
# 18.0.145 is the Cloth Config build for the 1.21.5 line (was 17.0.144 on 1.21.4).
cloth_config_version=18.0.145

# --- 1.21.5 PORT NOTES ----------------------------------------------------------
# Create + Farmer's Delight (NeoForge) are STILL NOT published for 1.21.5, so their
# compat stays dropped:
#   * Create: compat code removed; silver_sheet + the c:plates tags stay (vanilla),
#     dormant until Create ships for 1.21.5+.
#   * Farmer's Delight (NeoForge): Silver Knife compiles against the local compile-only
#     KnifeItem stub (no FD jar). Knife = plain tool-component fallback until FD ships.
# FD Refabricated (Fabric) is consumed via the same stub, so no FDR jar is needed.
#
# AppleSkin: DROPPED — no 1.21.4/1.21.5 build. Re-add appleskin_*_version if one ships.
#
# JEI: NeoForge only (21.4.0.27). EMI + JEI-Fabric: still deferred — no confirmed
# 1.21.5 build surfaced. Re-add the Fabric recipe-viewer wiring once available.
#
# 1.21.5 API: SwordItem/PickaxeItem/DiggerItem/ArmorItem/AnimalArmorItem REMOVED —
# tools/armor are now plain Item + Item.Properties component helpers (.sword/.pickaxe/
# .axe/.shovel/.hoe/.humanoidArmor/.horseArmor). pack_format: resource 55, data 71.
# --------------------------------------------------------------------------------
