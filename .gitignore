# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# claude
.claude

# cache
# Unanchored so it also catches the multiloader layout other branches use
# (common/src/generated/resources/.cache). Committing a HashCache from another
# branch makes a later runData silently skip writes.
**/.cache/

# Build leftovers from the multiloader branches' architectury setup
.architectury-transformer/

# other
eclipse
run
runs
run-data

repo