# Tells Architectury Loom that this subproject targets the NeoForge platform.
# This is what registers the `neoForge` dependency configuration used in build.gradle
# (loom does not auto-detect the platform from the architectury { } block).
loom.platform=neoforge
