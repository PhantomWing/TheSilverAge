# The Silver Age — NeoForge access transformer.
# (26.1 removed FireworkStarRecipe.SHAPE_BY_ITEM — the shape->ingredient mapping is now
#  per-recipe data; firework-star parity moved to a recipe override, so the AT entry for
#  that static field was dropped. See com.phantomwing.thesilverage.firework.ModFireworks.)
# Mirrors the common access widener entry widening the BlockEntityType ctor
# (1.21.2 removed BlockEntityType.Builder); used by shared ModBlockEntityTypes.
public net.minecraft.world.level.block.entity.BlockEntityType <init>(Lnet/minecraft/world/level/block/entity/BlockEntityType$BlockEntitySupplier;Ljava/util/Set;)V
# Mirrors the common access widener entry for BlockSetType.register (used by
# ModBlockSetTypes). 26.1/loom-no-remap has no remapJar, so there is no automatic
# AW->AT conversion; this whole file is the hand-maintained AT that mirrors every
# entry of common's thesilverage.accesswidener (NeoForge loads it from META-INF).
public net.minecraft.world.level.block.state.properties.BlockSetType register(Lnet/minecraft/world/level/block/state/properties/BlockSetType;)Lnet/minecraft/world/level/block/state/properties/BlockSetType;
# Mirrors the common AW entry widening RangeSelectItemModelProperties.ID_MAPPER,
# used by the shared common client.ModItemProperties to register the custom
# thesilverage:moon_phase range-select property (1.21.4 replaced the old
# ItemProperties.register system). Without this, the AW applies in dev / Fabric
# production but NOT in NeoForge production, throwing IllegalAccessError at
# moon-phase property registration on a production NeoForge client.
public net.minecraft.client.renderer.item.properties.numeric.RangeSelectItemModelProperties ID_MAPPER
