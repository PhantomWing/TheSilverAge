package vectorwing.farmersdelight.common.item;

import net.minecraft.world.item.Item;
import net.minecraft.world.item.ToolMaterial;

// Compile-time stub of Farmer's Delight's KnifeItem, supplying only the constructor
// signature so SilverKnifeItem compiles. Stripped from the shipped jar (shadowJar excludes vectorwing/**).
public class KnifeItem extends Item {
    public KnifeItem(ToolMaterial material, Item.Properties properties) {
        super(properties);
    }
}
